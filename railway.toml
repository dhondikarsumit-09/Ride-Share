[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway-backend"
watchPatterns = ["ridesharelive-backend-main/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
